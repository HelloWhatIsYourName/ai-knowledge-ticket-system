<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aiticket.vector.VectorSpikeMapper">
    <insert id="insert">
        INSERT INTO vector_spike (content, embedding)
        VALUES (#{content}, TO_VECTOR(#{vectorLiteral}))
    </insert>

    <select id="search" resultType="com.example.aiticket.vector.VectorSpikeRecord">
        SELECT id,
               content,
               VECTOR_DISTANCE(embedding, TO_VECTOR(#{queryVectorLiteral}), COSINE) AS distance
        FROM vector_spike
        ORDER BY distance
        FETCH FIRST #{limit} ROWS ONLY
    </select>
</mapper>
